# Active tick client (comparison fixture)

A verbatim copy of the operator's in-use scripted client, taken 2026-09-09 for Arc 4 item 7.7.3: `doitlist-tick.py` completes one Task, ticks its checkbox in the plan mirror, appends a changelog line, and prints the next open item, over the `doitlist_api.py` client. It is the baseline the companion skill's `done --mirror` is measured against. Not run by the test suite; not maintained here.
