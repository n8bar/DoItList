# m02.09 — Client-Interaction Foundation

_Parent: [`m02-ux-buildout.md`](m02-ux-buildout.md)_

> **Make life not suck on LongPoll.**

Arc 9 brings the whole app into compliance with the **client-first interaction** guardrails — [`UX_GUARDRAILS`](../../UX_GUARDRAILS.md) §6 (action feedback, acknowledge-every-action, interactive-from-first-paint, any-transport) and §7.3–7.4 (client owns ephemeral UI state + optimistic; presence continuity) — and makes them hold on LongPoll, not just WebSocket. It's the deliberate build of the **client half** we'd been retrofitting one §6 violation at a time. **Not** a rewrite and **not** abandoning LiveView — it hardens the client layer on top of it. Arc 8 made point fixes (the client-side confirm sweep, the client-side editor open) and the transport groundwork is in place (`longPollFallbackMs` raised; a partial connect/reconnect re-assert added); this arc makes the re-assert robust and codifies the pattern.

**The problem, precisely (measured, not theorized).** On a WebSocket session the page **paints in ~0.1s** (tree, members, panes — looks fully ready) but the LiveView isn't **live until ~2.9s**: a **~2.8-second dead window** where the page is a static photograph and nothing the user does takes. The connect *taking over* at the end of that window is what's felt as the "major reload" — and that takeover is what stomps client state opened during the window. There is **no second render that re-stomps the whole page**; the villain is the **painted→interactive gap**, not a re-render. **Transport was never the villain** — WebSocket only *shrank* the gap (warm LongPoll is ~25% slower, not the earlier multi-second disaster, which was a cold/slow connect stacked on LongPoll). The fix is to **close the paint→interactive gap and make the in-between fully usable, on any transport** — don't re-chase the transport.

**Action inventory.** The spine of this arc — one row per direct-manipulation action. Populated by worklist 1.1, then read by worklist 3's fixes and worklist 4's dead-window handling.

_Worklist 1.1 + 1.2 output. 86 actions inventoried; 29 flagged as §6 violations. **Pre-connect** records behavior **today**: `lost` = silently dropped pre-connect, `optimistic` = client-applied & replayed, `queue`/`signify` = not yet present anywhere — that absence is the §6.8 dead-window worklist 4 closes. The **3.1 archive-confirm seed is refuted as a round-trip** — the owner-path confirm opens client-side (`app.js:1066-1069`), zero network; the lived ~4-beat latency is a client-side slow-open, diagnosed separately. Direct-manipulation actions are listed first, server-gated forms last._

| Action | Feedback immediate? | State (client / server) | Pre-connect behavior | §6 status |
|---|---|---|---|---|
| Select task (row click) | yes | client | optimistic (replayed on connect) | OK |
| Deselect / close task details | yes | client | optimistic | OK |
| Open/close task Details pane (via selection) | yes | client | optimistic | OK |
| Keyboard arrow navigation (selection move) | yes | client | unknown (no pre-connect hook) | OK |
| Open initiative editor (click title) | yes | client | optimistic | OK (Arc 8 client-side) |
| Close initiative editor (X / rail close) | yes | client | optimistic | OK |
| Expand / collapse tree node (chevron / Space) | yes | client | optimistic (localStorage) | OK |
| Open add-task form (root / n / s / +) | yes | client | optimistic | OK |
| Move add-form between slots (Up/Down typing) | yes | client | optimistic | OK |
| Focus a pane field (pill tap / Alt+P/W/A) | yes | client | optimistic | OK |
| Open/close pane `<details>` disclosure (KeepOpen) | yes | client | optimistic | OK |
| Open/close add-member form (`<details>`) | yes | client | optimistic | OK |
| Open/close delete-account confirm (`<details>`) | yes | client | optimistic | OK |
| Open Collaborators rail popover (native Popover) | yes | client | optimistic | OK |
| Open/close live Chat panel (rail toggle) | yes | client | optimistic | OK |
| Cycle/select color theme | yes | mixed | optimistic | OK |
| Update task inline (title/desc/priority/assignee/progress) | yes | mixed | lost | OK |
| Set progress via slider | yes | mixed | lost | OK |
| Toggle leaf completion (checkbox) | yes | mixed | lost | OK (commit lost pre-connect) |
| Toggle branch completion / cascade (client confirm) | yes | mixed | lost | OK (Arc 8 client confirm) |
| Delete task (Del key / button, client confirm) | yes | mixed | lost | OK |
| Drag reorder / move task in tree (predicted-flip confirm) | yes | mixed | lost | OK |
| Add co-assignee (pane dropdown) | yes | mixed | lost | OK |
| Remove co-assignee | yes | mixed | lost | OK |
| Reorder co-assignees (up/down) | yes | mixed | lost | OK |
| Drag member onto task row (assign primary/co) | yes | mixed | lost | OK |
| Undo (Ctrl/Cmd+Z / toolbar) | yes | server | lost | OK |
| Redo (Ctrl+Y / Ctrl+Shift+Z / toolbar) | yes | server | lost | OK |
| Confirm/cancel server-gated pending action (modal) | yes | mixed | unknown | OK |
| Keyboard reorder/reparent (Alt+Arrow) | partial (saving hue) | server | unknown | OK |
| Keyboard step priority/assignee (P/A) | partial (saving hue) | server | unknown | OK |
| Set sort mode on a branch (dropdown) | partial (saving hue) | server | lost | OK |
| Change sort mode / reverse (index, InitiativeSort) | yes | mixed | lost | OK |
| Drag-reorder initiative cards (InitiativeDrag) | partial (drag/placeholder only; no drop-time reorder) | mixed | lost | VIOLATES 6.2 (no drop-time optimism) |
| Leave Initiative (own-row, client confirm) | partial (leaving latch) | server | lost | OK |
| Toggle "Show-hidden" (Archived panel checkbox) | no | server | queue | VIOLATES 6.5 |
| Toggle "Show completed" reveal (Assigned) | partial (native box only) | server | lost | VIOLATES 6.5/6.2 |
| Toggle "Show archived & hidden" reveal (Assigned) | partial (native box only) | server | lost | VIOLATES 6.5/6.2 |
| Toggle "Group by Initiative" (Assigned) | partial (native box only) | mixed | lost | VIOLATES 6.5/6.2 |
| Dismiss archive-on-completion prompt (banner X) | no | client | queue | VIOLATES 6.5 |
| Cancel hand-off (dismiss modal) | no | server | lost | VIOLATES 6.5 |
| Open comment inline editor (Edit own comment) | no | client | lost | VIOLATES 6.5 |
| Cancel comment edit | no | client | lost | VIOLATES 6.5 |
| Show comment edit-history popup | no | mixed | lost | VIOLATES 6.5 |
| Hide comment edit-history popup | no | client | lost | VIOLATES 6.5 |
| Cascade sort to descendants (cascade_sort, >10 branches) | partial (saving hue) | server | lost | VIOLATES 6.5 |
| Archive initiative — open confirm dialog (owner path, 3.1 seed) | yes | client | lost (commit only) | OK (confirm opens client-side) |
| Archive initiative — member path (server backstop) | no | server | lost | OK (server data, static body) |
| Archive initiative — commit (Proceed / no-confirm) | no | server | lost | VIOLATES 6.7 |
| Hide initiative (show-page Hide button) | no | server | queue | VIOLATES 6.7 |
| Unhide initiative (Archived panel) | no | server | queue | VIOLATES 6.7 |
| Unarchive / Restore initiative (Archived panel) | no | server | queue | VIOLATES 6.7 |
| Restore initiative from Trash | no | server | queue | VIOLATES 6.7 |
| set_index_style (task numbering select, show page) | no | server | queue | VIOLATES 6.7 |
| Edit initiative subtitle (settings panel) | partial (typing only) | server | lost | VIOLATES 6.7 |
| Change progress-calc mode (settings panel) | no | server | lost | VIOLATES 6.7/6.1 |
| Toggle Viewer+ policy (settings panel) | no | server | lost | VIOLATES 6.7 |
| Add comment (add_comment form) | no | server | lost | VIOLATES 6.7 |
| Save edited comment | no | server | lost | VIOLATES 6.7 |
| Add collaborator to Initiative (rail menu) | no | server | lost | VIOLATES 6.7 |
| Drag collaborator onto Initiative (add as viewer) | partial (gesture only) | server | lost | VIOLATES 6.2/6.7 |
| Remove past Collaborator (rail inline confirm) | no | server | lost | VIOLATES 6.7 |
| Remove member via Collaborators rail menu | no | server | lost | VIOLATES 6.7 |
| Send chat message | no | server | lost | VIOLATES 6.7 |
| **Dead-window (cross-cutting):** all server-gated writes route through `DoitPush`, defined only after `TaskKeys` mounts | partial | server | lost | VIOLATES 6.8 |
| Open an initiative (index → show navigate) | partial (nav affordance) | server | unknown | OK (navigation) |
| Open assigned task (row navigate / deep-link) | partial (nav affordance) | server | optimistic (anchor) | OK (navigation) |
| Open New Initiative form (toggle `<details>`) | yes | client | lost (persistence only) | OK |
| Create task (submit add-task form) | partial (phx-disable-with) | server | lost | OK |
| Create initiative (submit form) | partial (phx-disable-with) | server | queue | OK |
| set_sort (per-task sort, show page) | partial (saving hue) | server | queue | OK |
| Purge initiative (Trash, data-confirm) | partial (native confirm) | server | queue | OK |
| Change member role (editor↔viewer) | partial (native select + flash) | server | lost | OK |
| Add member (members panel form) | yes (phx-disable-with) | server | lost | OK |
| Remove member (X → client confirm) | yes (client confirm) | server | lost | OK |
| Confirm hand-off (member removal w/ assignments) | yes (phx-disable-with) | server | lost | OK |
| Save profile (display name + email) | yes (phx-disable-with) | server | lost | OK |
| Validate profile (live validation) | no | server | lost | OK (background) |
| Save username | yes (phx-disable-with) | server | lost | OK |
| Validate username | no | server | lost | OK (background) |
| Save password (change password) | yes (phx-disable-with) | server | lost | OK |
| Validate password | no | server | lost | OK (background) |
| Save preferences (batched) | yes (phx-disable-with) | server | lost | OK |
| Validate preferences (live change) | partial (native control) | mixed | lost | OK |
| Delete account | yes (phx-disable-with + client confirm) | server | lost | OK |
| Reset confirmation prompts (clear localStorage) | yes | client | lost | OK |

1. [x] **Inventory & classify the direct-manipulation actions**

	A systematic pass over every direct-manipulation action — catalogue them, then classify each against §6 — so the fixes (worklist 3) are populated from real findings, not memory. The spine the rest of the arc reads.

	1. [x] **Inventory direct-manipulation actions** into a committed table — the **spine of this arc**: worklist 3 draws its fix subitems from it, and worklist 4 drives the dead-window handling from the same columns.
		1. [x] **Enumerate the actions** — every direct-manipulation interaction (selection, expand/collapse, focus, editor/details open-close, optimistic flips, add-form open, reorder/move), one row each.
		2. [x] **Record the three columns per action:** feedback immediate? · state location (client vs. server) · pre-connect behavior (optimistic / queue / signify).
		3. [x] **Commit the table** as the shared artifact the rest of the arc reads.
	2. [x] **Classify against §6.** For each inventoried action, mark whether it satisfies §6.2 (optimistic for fast ops), §6.5 (view-state never waits on network), and §6.6 (confirm preserves optimism); flag any that still gate input or feedback on a round trip.

2. [x] **Ephemeral UI state survives the LiveView lifecycle**

	Client-owned UI state (selection, open panes, expand/collapse, optimistic flips, presence, focus/caret/scroll position) must survive every server re-render, reconnect, and initial connect. Reapply it *inside* the morphdom patch via LiveView's `dom` callbacks — preventively, so it's never clobbered and there's no race — driven by a declarative attribute convention and a small client-side store of the truth. One transport-agnostic path, replacing the per-feature observers and `KeepOpen`.

	1. [x] **Define the client-owned convention + unified state store.** A declarative, **server-rendered** marker (`data-keep="<kind>"`) for what's client-owned — selection, open panes, expand/collapse, optimistic flips, presence, and focus/caret/scroll position — plus a single JS store of the truth (`window.DoitState`) that survives re-renders and seeds re-added elements, replacing the eight fragmented `window.Doit*` objects.
	2. [x] **Apply it through the patch.** `onBeforeElUpdated` carries the store's truth onto elements updated in place (reconcile the incoming node *before* morphdom commits, so the wrong value never paints); `onNodeAdded` re-applies from the store onto elements morphdom removes and re-adds (reorder, reset re-stream, the initial-join replace). Together they cover both cases; handle attribute removal too, and preserve focus/caret/scroll — the highest-stakes state — through the patch.
	3. [x] **Migrate existing state onto the convention** — each feature routes its truth through `DoitState` and renders a `data-keep` marker; old paths stay until 2.4.
		1. [x] **Selection**
		2. [x] **Editor pane** — pane visibility done; the mobile rail (`#details-rail`) + backdrop (`#pane-backdrop`) still render only from `@selected_task_id` (no `data-keep`) and ride the legacy `syncRail` — migrate before 2.4 (audit: the editor-open flyout collapses once the legacy path retires). → **Covered** (additive): a `data-keep="rail"` kind reconciles the rail's `data-open` + the backdrop `hidden` from selection/editor state; legacy `syncRail` still runs. Verify at the 2.4 observer-off eye-pass.
		3. [x] **Expand/collapse**
		4. [x] **Presence badges** — repaint rides entirely on the legacy MutationObserver (the `presence` anchor is inert; per-row slots carry no `data-keep`) — give it a real post-patch trigger (or per-slot kind) before 2.4 or presence breaks silently on retire. → **Covered** (additive): `onPatchEnd` drives `applyPresenceBadges()` after every patch; legacy observer still runs. Verify at the 2.4 observer-off eye-pass.
		5. [x] **Completion-flip (toggle) optimism** — also syncs the pane manual-slider on checkoff (finding 1).
		6. [x] **Drag-move optimism** — truth routed through `DoitState.pending.move`; its structural re-apply stays on the legacy path, dom-callback wiring deferred to 2.4 (sentinel `data-keep` kind on `#task-tree`).
		7. [x] **Details disclosure (`KeepOpen` → `data-keep="open"`)** — `DoitState.detailsOpen` is never evicted, so a re-added `<details>` reopens from a stale entry (the Delete-account destructive confirm reappears expanded after nav) — add discard-eviction before 2.4. → **Covered** (additive): `onPatchEnd` prunes stale `DoitState.detailsOpen` entries so a re-added `<details>` falls back to the server default. Verify at the 2.4 observer-off eye-pass.
		8. [x] **Focus / caret / scroll** — focus/caret already covered by LiveView (stable ids + `restoreFocus`); filled the one real gap, inner-container scroll (`#tree-scroll`).
	4. [x] **Retire the racing observers, keep the store.** Remove the `DoitSelection` / `DoitInitiativeEditor` MutationObserver re-assert code and `KeepOpen`; the underlying state store stays — it's now the single source the `dom` callbacks read. **Blocked until 2.3's editor-rail/backdrop, presence, and details-eviction gaps close** — the ultracode audit found retiring now would silently break the mobile editor flyout, presence-on-morph, and reopen destructive confirms. Verify with a regression check run with the observer disabled. → **Done in code**: legacy machinery removed (MutationObserver, reassertClientState, connect guards, KeepOpen + CollapseChildren hooks, applyCollapseStates); the preventive path (`data-keep` kinds + `onPatchEnd`) is the sole mechanism; the `.TaskKeys` held-selection replay kept (pre-connect server fetch, not a DOM re-assert). Adversarially verified safe (immediate-apply intact, initial-join covered, zero dangling refs), 273 green. **Ticks — with 2.3's editor/presence/details — on the operator's observer-off eye-pass.**

3. [x] **Close the §6 gaps — optimistic & client-owned**

	One fix per flagged action, grouped below by shared mechanism, drawn from the inventory's 28 discrete-action violations; the cross-cutting §6.8 dead-window row is worklist 4, not duplicated here. Each makes feedback instant and the action client-owned where the state is ephemeral, and rides worklist 2's preserve path so the optimism isn't stomped. Acknowledge — do not redo — the Arc 8 client-side confirm sweep and the client-side initiative-editor open as already-passing. Pre-connect *survival* of these acknowledgements is worklist 4 (§6.8): this worklist builds the acknowledgement, worklist 4 makes it hold before connect.

	1. [x] **Archive-confirm slow-open (reframed seed — not a round-trip).** The owner-path confirm already opens client-side (`app.js:1066-1069`), so the lived ~4-beat latency is not a §6.5 network wait. Diagnose the client cause — the document-level click handler's `#task-tree [data-task-row]:not([data-done])` scan on a large tree, and/or the full-screen overlay's paint — and make the open instant. Diagnose against a real interaction, not a synthetic `el.click()`. → **Resolved**: operator no longer notices the latency; a real CDP render-path trace measured the `#archive-confirm` flip at ~5ms (single frame) and the suspected overlay-promotion fix A/B'd to a 0ms no-op (not shipped). Likely fixed incidentally by the WL2 work — no honest fix to make in the confirm path.
	2. [x] **Client-side view-state reveals (§6.5).** Flip at click on the client; the server assign only reconciles, never gates the reveal. Covers the Assigned-view show-completed / show-archived+hidden / group-by toggles (group headers computed client-side for the reflow), the Archived-panel show-hidden toggle, and the archive-on-completion banner dismiss. → **Done**: split three ways. **Group-by** is a pure client-side reflow (§6.5) — the list always renders both the per-Initiative group headers and the per-row subtitles, and the wrapper's `data-grouped` (flipped at the click) drives unlayered CSS that shows the right set; the pref only persists on the round-trip. **Banner dismiss** is client-owned — hidden at the click, held by a `data-keep` applier until the server drops the element, with `onPatchEnd` clearing the flag so a fresh 100% crossing re-raises. The **three reveal toggles** are genuinely server-gated (rows filtered out of the DOM), so the fetch stays and the click is acknowledged with an in-flight signifier (§6.7): optimistic tick held by a `reveal-toggle` applier + `aria-busy` + a `phx-click-loading`-driven spinner in a reserved fixed-width slot (no phantom rows). 274 green, assets clean.
	3. [x] **Client-owned open/close for editors & popovers (§6.5).** Open/close at click; the server assign reconciles. Covers the comment inline editor open + cancel; the comment edit-history popup (open immediately with a loading state while versions fetch — don't gate the open on the fetch — close client-side); and the hand-off-cancel modal close. → **Done**: re-run with the versions newest-first preload fix; adversarially verified, 273 green.
	4. [x] **Convert the remaining server-raised confirms to client-opened (§6.5/§6.7).** The eye-pass + diagnosis found this is NOT a lone modal — three confirms still round-trip before they appear: (a) **cascade-sort** (>10 branches) — server `@pending_action` (initiative_show_live.ex:1030-1049); (b) **member-handoff** (remove member w/ assignments) — server `@pending_handoff` (:1456-1461), opens only after the round-trip; (c) **move/create completion-confirm backstop** — the server `@pending_action` can fire as the FIRST modal instead of the client `#move-flip-confirm` / `#cascade-confirm`. Convert each to the Arc-8 static client-opened pattern (predict counts from the DOM, open synchronously, server authoritative on commit). For handoff, whose body needs server data (assignment count), at minimum show an in-flight signifier at click and fetch only what isn't client-known.
	5. [x] **Drop-time optimism for drags (§6.2).** Apply the result at drop, snapshot/revert on failure. Covers the InitiativeDrag card reorder and drag-collaborator-onto-initiative (optimistic viewer chip). → **Done**: three parts. **Fix A** — the InitiativeDrag card reorder now applies the new order to `#initiatives` at drop and holds it on the preserve path (`DoitState.pending.initiativeOrder` re-asserted by `applyPendingInitiativeOrder` from `onPatchEnd`, mirroring the structural drag-move hold); `apply_sort` replies `ok` so the hold releases on success, reverts to the prior snapshot + bonks on failure, and self-heals a dropped reply on an 8s timer. **Rail avatar row** — each rail Initiative entry now renders a width-filling, capped (6) member-avatar stack with a `+N` overflow and online dots (members batch-attached in `list_visible_initiatives`, both themes, count as the a11y label with decorative avatars). **Fix B** — a drag-collaborator-onto-initiative drop inserts the dropped user's dimmed avatar chip into that entry's row at drop, held by the `rail-avatars` KeepRegistry applier (deduped by `data-member-id`) so a mid-flight patch can't stomp it; `add_collaborator_to` now refreshes the rail initiatives so the server render carries the real avatar, and the reply pulls the stand-in (ok) or pulls + bonks (failure — MUST NOT LIE). 276 tests green, assets build clean.
	6. [x] **Optimistic echo for server-gated writes that show their own result (§6.7).** Apply at submit, reconcile/dedupe on the reply. Covers send-chat-message (own-line echo), add-comment (bubble) and save-edited-comment (text), and the collaborator-rail add (insert) and remove (past-collaborator + rail-menu — route the rail-menu path through the client confirm).
	7. [x] **In-flight signifier for the rest (§6.7).** Where optimism doesn't fit, signal in-flight at click plus a success ack where the effect is invisible. Covers hide / unhide / unarchive / restore initiative and the archive commit (mirror `leave_initiative`'s latch — label + disable + safety timer); `set_index_style` (optimistic relabel or saving hue); and settings — subtitle (saved tick), progress-calc (saving hue across the tree; §6.1 heavy recompute), viewer+ (in-flight + success).
	8. [x] **Info-hint icon client-toggle (§6.5).** The Manual-progress "i" icon is server-`:if`-rendered (branch-only), so the branch↔leaf switch waits on the round-trip. Render it always and flip an `invisible` class client-side from the DOM branch signal (the children `<ul>`), exactly like its sibling notes in `fillPaneFields` — zero round-trip, no `data-keep` kind.

4. [x] **Input always registers — no "looks ready but isn't" dead window**

	The user must see their input did something up front. Eliminate the gap where the page is painted but not yet interactive and actions are silently lost.

	1. [x] **Connecting / in-flight signifier.** The page never looks fully interactive before it is.
		1. [x] **Detect + render.** Track connect / in-flight state via the socket lifecycle (`onOpen` / `onClose`, `phx:page-loading-start` / `-stop`) and show a signifier — extend the existing topbar (already wired to `phx:page-loading-*`); clear when live/settled; a11y: not color-only, announced. → **Done**: a server-rendered `#conn-status` badge (`Layouts.app`, hidden + inert by default) wired in app.js's topbar block off the SOCKET lifecycle (`onOpen`/`onClose`/`onError` — fires on WS *and* LongPoll, §6.9) plus `phx:page-loading-stop` kind `initial` for the precise channel-join "now live" moment. Shows "Connecting…" through the pre-connect dead window and "Reconnecting…" on a dropped connection — DISTINCT from the topbar bar (which stays the ordinary navigate/patch/submit flash) — and clears once live. `role="status"` + `aria-live="polite"` announce each change via the text (not color-only); the spinning hero-icon is the shape cue, `motion-safe` so reduced-motion gets a static legible badge; both themes; never shown on dead views (JS only reveals it on a `[data-phx-main]` page). 276 green, assets clean. Lived light/dark + LongPoll eye-pass rides WL7 (7.3.4/7.3.5).
	2. [x] **No dropped pre-connect actions.**
		1. [x] **Capture-and-replay path.** `window.DoitPush` exists from app.js load: live → dispatch immediately through the mounted root hook's bound `pushEvent`; dead window → capture onto an ordered `DoitState.preconnectQueue`. Both LiveViews register a live backend (show `.TaskKeys`, index `.IndexLive`); registering on mount is the flush trigger. Coalesce singleton intents (view prefs, per-task field/move) last-write-wins at a stable position; append distinct acts (creates, comments) in order; skip selection — the `.TaskKeys` mount replays the final one. The optimistic / in-flight ack already stands at the click; only the server-gated push is held.
		2. [x] **Capture every server-gated action in the dead window, not only the `DoitPush`-routed ones.** Add a live-gated capture-phase document listener (active only while not yet live) that records native `phx-click` (+`phx-value-*`) / `phx-submit` / `phx-change` intents into the same queue and flushes them through the funnel on connect — covering inline `update_task`, the progress slider, branch `set_sort`, the Assigned reveal/group-by prefs, Archived show-hidden + hide/unhide/unarchive/restore, `set_index_style`, settings subtitle/progress-calc/viewer+, the common add-task submit, and add/save-comment. Hook-drag gestures stay out — their hooks mount only once connected.
		3. [x] **Flush on connect.** Registering the live backend on root-hook mount replays the queue in order, then clears it (cleared before dispatch so a re-entrant push doesn't re-flush). Reply callbacks + KeepRegistry appliers reconcile each event; a refusal reverts. The backend stays set for the view's life, so a reconnect never re-flushes committed work.
	3. [x] **Up-front acknowledgement.**
		1. [x] **Wire instant acknowledgement for each flagged action** — for every action the 1.1 inventory marks not-immediate, acknowledge at the moment of the action (optimistic apply or in-flight signifier), independent of connect state. A shared `latchButton` client latch (label + disable + `aria-busy` + pulse, ~8s safety restore) fires at the gesture via module-load delegated click/submit listeners — so the cue shows in the dead window too — on Hide / unhide / unarchive-restore / restore-from-trash and the Create-initiative / Add-member / Save-comment / account-save submits (replacing their live-only `phx-disable-with`); the three reveal-toggle spinners get a `doit-reveal-busy` twin of the `phx-click-loading` rule. `disabled` is deferred one microtask so it never suppresses the push it acknowledges. (Show-page Hide had no cue even when live — closed here.)

5. [ ] **Seamless navigation — master-detail, no remount**

	Opening an initiative from the workspace should patch the detail region, not remount and swap the whole page. The workspace list and initiative view become one LiveView (or one `live_session`) navigated by `push_patch` — the established collaborative-app pattern that keeps the live session, and your presence, intact instead of flickering you out and back in for everyone else viewing.

	1. [x] **Map the current two LiveViews.** How `InitiativeIndexLive` and `InitiativeShowLive` are structured today — mounts, assigns, subscriptions, presence, routes, shared components — so the merge is grounded in the real code, not guessed.
	2. [x] **Design the merge — inline master-detail, one LiveView.** Both routes (`/initiatives` `:index`, `/initiatives/:id` `:show`) serve from one kept-mounted shell LiveView in the same `live_session`; list↔detail is a `push_patch` driving `handle_params`, no remount. The shell mounts once and holds the cross-page concerns: global presence (tracked once, stays), notifications/theme, the rail data, and the single dead-window `livePush` registration (one shell root hook — ends the per-hop registration churn). `handle_params` switches: `:index` tears the detail down (unsubscribe Tasks/presence/chat for the prior id, untrack per-initiative presence, clear the show-only assigns) and renders the list; `:show` enters the detail (subscribe + track for the id, `load_tree` + the deferred loads, set the show-only assigns, honor the `?task=` deep-link); an initiative→initiative switch leaves the old then enters the new. Only one of list/detail renders at a time, so the detail-only and list-only `data-keep` kinds never share the DOM. The detail's render + event-handler bodies move into plain function modules the shell delegates to (no LiveComponent — it wouldn't own the subscriptions anyway). `<.link navigate>` list↔detail become `<.link patch>`; the mount-guard / removed-member ejects become `push_patch` + flash. The `.TaskKeys` selection-replay + deep-link + undo/redo-key duties bind to the detail region's lifecycle.
	3. [ ] **Implement it.** Route workspace↔initiative navigation through `push_patch` / `handle_params` into the kept-mounted shell; the socket, PubSub subscriptions, and presence stay intact across navigation — no re-track, no flicker for other viewers.
	4. [ ] **Instant, non-stomping open.** Opening patches only the detail region — instant view-state plus an optimistic / in-flight fill — and rides worklist 2's preserve path so it never stomps client state.

6. [ ] **Usability**

	Usability gaps surfaced from real use (watching a user) and the eye-pass — small, discrete fixes outside the §6 / preserve-path technical work.

	1. [ ] **Password peek toggle** — add a show/hide (eye) toggle to the password field on login (`user_session_html/new`) and registration (`user_registration_html/new`); users can't verify what they typed. Controller HTML forms, not LiveView → a small client-side toggle.
	2. [ ] **Selection highlight under the saving hue.** A selected row in the `.is-saving` (pink) state still looks unselected — the pink `!important` background overrides the no-`!important` emerald selection highlight (selection actually succeeds; only the paint is lost). Add a higher-specificity `li[data-selected] > [data-task-row].is-saving` rule (+ dark / hover variants). Pure CSS.
	3. [ ] **Open the new initiative on creation.** Creating an initiative lands you back on the workspace list, where you must tap the new card to enter it — users hesitate at the "what now?" beat. Navigate straight into the new initiative on create. (Touches the create handler; relates to worklist 5 navigation.)
	4. [ ] **Empty-initiative copy points to New List.** Inside an initiative with no lists, the empty-state placeholder should tell the user to use the **New List** button above — it currently doesn't say what to do next.
	5. [ ] **Chat caret offset on iOS.** The live-chat input's text caret sits several spaces right of the typed text — iPhone only (likely input `letter-spacing`, a transform, or a font quirk). Best-effort fix diagnosed from the CSS at the operator's request; **needs a real-device confirm** (operator has no iOS device, not verifiable on desktop).
	6. [ ] **iOS auto-zoom on input focus.** On iPhone, focusing an input zooms the page in and never zooms back, pushing UI the mobile UX relies on off-screen — the canonical iOS Safari behavior when an input's font-size is < 16px. Fix: inputs/textareas/selects ≥ 16px on mobile (confirm the viewport meta is `width=device-width, initial-scale=1`; no zoom-disabling — a11y). Needs a real-device confirm.
	7. [ ] **Initiative-title edit signifier.** The title's only cue was a too-subtle dotted underline (a mobile user missed that the title is editable). Make the **grove icon + title name a single subtle button** — persistent soft border + rounded corners on the *current* background (reads as tappable without hover, for mobile), faint hover tint + pressed state, both themes; drop the dotted underline (+ its now-pointless `.editor-open` text-decoration suppressor). Title only (not subtitle). A button reads "tap to open," matching that the click opens the details *pane* (not inline edit). Gate the button look on `@can_edit`; proper button a11y, h1 heading preserved. _First pass committed (subtle border + hover/pressed) but it doesn't read as a button to the operator — revisit the treatment when we return; deferred to stay on the WL2 sequence._

7. [ ] **Testing & Verification**

	1. [ ] **Audit & anti-regression.**
		1. [ ] **Whole-app §6 sweep.** Walk the full app against [`UX_GUARDRAILS`](../../UX_GUARDRAILS.md) §6 (6.1 progress < ~100ms, 6.2 optimistic, 6.5 view-state off the network, 6.6 confirm preserves optimism) and record findings.
		2. [ ] **Codify the client-first pattern.** Document the client-first interaction pattern (where ephemeral state lives, how optimistic writes reconcile, how new UI joins the re-assert path) so new features inherit it instead of re-introducing round-trips. Note the guardrail in [`UX_GUARDRAILS`](../../UX_GUARDRAILS.md) §6 if a line is missing.
	2. [ ] **Tests.** `mix test` coverage for any new client/server contract surfaced by the re-assert mechanism and the in-flight signifier (the automated gate per house rules).
	3. [ ] **Usability testing** — [Human] / bridge eye-pass once built; mix tests ride each impl item.
		1. [ ] [Human] Open a pane (details or initiative editor) immediately on page load and confirm it survives the connect — the known pane-vanish regression (bridge-verified failing today). Confirm on **LongPoll**, where it surfaced, not just WebSocket.
		2. [ ] [Human] Reconnect (drop and restore the socket) with selection + an open pane + an expand/collapse state set; all ephemeral UI state is restored after reconnect.
		3. [ ] [Human] Input feedback is immediate on every direct-manipulation action — selection, expand/collapse, editor open, optimistic flip — with no visible wait on the network.
		4. [ ] [Human] During the pre-connect window the page shows a connecting/in-flight signifier; an action taken then is acknowledged and not silently dropped once connected.
		5. [ ] [Human] Light + dark: the connecting / in-flight signifier reads correctly in both modes.

	8. [ ] **Danger-Zone card needs bottom margin.** The account “Danger zone” card (account_live.ex:424, the delete-account confirm) sits flush to the bottom — add ~16px (`mb-4`) below it. Trivial CSS; surfaced in the WL2 eye-pass.

## Cross-cutting requirements

- **Spec compliance (WHAT vs HOW).** The end-state is the spec — [`UX_GUARDRAILS`](../../UX_GUARDRAILS.md) §6 + §7.3–7.4; this arc is the HOW that brings the app to it. The standing release gate in [`CLAUDE.md`](../../../CLAUDE.md) (Engineering Style) keeps new features inside it. (Subsumes the old LongPoll-parity, no-round-trip-before-input, and not-a-rewrite notes — now in the spec / intro.)
- **One preserve path.** Worklist 2's `onBeforeElUpdated` hook is the single owner of keeping client-owned state through patches/reconnects/initial connect — preventive, not corrective; per-feature guards collapse into it (worklist 2 item 4).
- **A11y.** The connecting / in-flight signifier is perceivable (not color-only) and announced; restored focus after a patch lands where the user left it.
