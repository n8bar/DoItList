# m02.03 — Task Tree: Reorganization & Layout
_Parent: [`m02-ux-buildout.md`](m02-ux-buildout.md)_

Reshaping and laying out the task tree within an Initiative: drag / keyboard reorganization (per [`ProductSpec.md`](../../ProductSpec.md) § Reorganization), the single-root-task model, and responsive tree layout (per [`ProductSpec.md`](../../ProductSpec.md) § Task Tree Display). Cross-Initiative moves are out of scope.

## 1 — Drag & drop

1. [x] **Drag handle on each task row.** A 3-vertical-dots glyph at the far left of the row (left of the chevron / checkbox) is the only place a drag starts. Prevents accidental drags from row-body clicks. Handle has its own touch target meeting the 44×44 px rule from UX_GUARDRAILS.
2. [x] **Persistence + roll-up recompute.** Every drop computes new `parent_id` + `sort_order`, persists in a single transaction, and recomputes ancestor progress for both the old and new parent chains. Failures roll back the entire drop and surface a flash; the UI reverts to pre-drag state. Lands as a `Tasks.move_task/3` context function before any gesture wiring so items .03.01.03–.07 can call into a real backend rather than stubs.
3. [x] **Vertical drag → reparent.** Dropping task A onto task B's row makes A a child of B (appended at the end of B's children). Drop target highlights during hover. After commit, ancestor progress recomputes for both old and new parent chains (via .03.01.02).
4. [x] ~~**Horizontal drag → indent / dedent.**~~ While dragging, horizontal cursor position controls depth: dragging right past a threshold indents A under its previous sibling; dragging left dedents A toward an ancestor's level. A subtle indent guide tracks the cursor so the target depth is visible before drop. _Superseded by .03.03.06._
5. [x] ~~**Snap-back zone.**~~ A horizontal cursor band inside the row that means "no change" — letting go inside that band aborts the move with no commit. Lets the user back out mid-drag without committing a wrong drop. _Superseded by .03.03.06._
6. [x] **Keyboard alternative — reuse task-selected state.** A task is "selected" when its Details panel is open (the existing selection model from Arc 2 item 6 / 21). While a task is selected, keyboard shortcuts reorder and reparent that task: `Alt+↑/↓` reorders within siblings; `Alt+←/→` dedents / indents. No mouse needed; no separate "drag focus" concept — selection IS the keyboard focus for reorganization.
7. [x] **Cross-list moves within the same Initiative.** Dragging across List trees changes A's root List. Cross-Initiative moves remain out of scope (ProductSpec § Reorganization). Verified by the cross-list test in `test/doit/tasks_test.exs` (covers source / dest root recompute and the Initiative-level aggregate change).
8. [x] **Mobile touch — long-press to start drag.** Long-press (~400 ms) on the drag handle starts the drag; the rest of the gesture mirrors mouse behavior. Short taps on the handle do nothing — the handle is drag-only, not a button.
9. [x] **Completion-state confirmation modal.** Create + move actions that would auto-flip an ancestor's `status: done` (in either direction) prompt a confirmation listing the affected tasks before committing. Without this, a drag can silently uncomplete a parent (or auto-complete one) — too surprising. `Tasks.preview_create/2` and `Tasks.preview_move/3` dry-run via transaction rollback, snapshot ancestor chains before / after, and classify as scenario 1 (would uncomplete), 2 (would complete), 3 (both), or `nil` (no flip). The modal renders in 1/2/3; `nil` commits straight through. Driven by a `:pending_action` socket assign + `confirm_pending` / `cancel_pending` events. Side effect: branch progress reconciliation moves out of `Progress.compute` (which used to short-circuit `status: done → 100`) and into `Tasks` proper — branches always derive from children, so a parent that gains an incomplete child reflects truth instead of masking it. Branches still reach 100 once all leaves do.
10. [x] ~~**Drop-zone two-mode redesign.**~~ A two-mode model selected by vertical distance from the source row's center: horizontal mode (within ±~1.5× row height of source center) treats horizontal cursor position as indent / dedent — .03.01.04 + .03.01.05 behaviors apply; vertical mode (cursor moved off the source's vertical slot) treats the row under the cursor as anchor, drops as child-of-anchor — .03.01.03 behavior applies. Wider snap-back band (64 px each side) in horizontal mode. Tunable constants live in `assets/js/app.js`. _Superseded by .03.03.06 — only the vertical-mode half survives; horizontal mode and its tuning constants are removed._
11. [x] **Suppressible, fully-styled confirmations.** Route every confirmation through one styled modal and let the user silence the non-destructive ones.
    - **One modal, all confirms.** Fold every confirmation into the `completion_confirm` modal's `pending_action` mechanism: existing `:move` / `:create` (completion-flip) and `:cascade_sort`, plus new `:cascade_complete` / `:cascade_incomplete` (branch checkbox) and `:delete_task` / `:delete_initiative`. The two native `data-confirm`s and the `DeleteTask` JS `confirm()` are removed — no native dialogs remain.
    - **Confirm classes**, each its own suppression key: `completion-flip` ("completion changes"), `cascade-sort` ("large branch reorgs"), `cascade-complete` ("branch completion changes"). The cascade-sort body reads e.g. _"This is a large branch reorg affecting X tasks."_ Deletes (`:delete_task`, `:delete_initiative`) are their own modal kinds but **not** a suppressible class.
    - **"Don't show this again for {class}"** checkbox on the three suppressible classes only, the wording naming the class (e.g. "Don't show this again for large branch reorgs"). Deletes never carry it.
    - **Per-class suppression in localStorage** — `doit:confirm-skip:<class>`. A mount hook reads the flags and pushes them to the LiveView (`@confirm_skips`); a suppressible action checks the assign and commits straight through instead of opening the modal. Checking the box on Proceed writes the flag (localStorage + the assign). Per the localStorage-now convention (.06.2 / BACKLOG), a future profile/settings page clears these.
    - **Optimistic delete survives.** Routing delete through the modal keeps the .03.03.08 optimistic row-removal — it fires on Proceed, not the (now-gone) JS confirm.
12. [x] **Confirm modals acknowledge the click.** Restores UX_GUARDRAILS 6.1/6.2 — today Proceed gives no feedback and the modal sits frozen until the write finishes.
    - **Pressed state.** `active:` styles on the modal's buttons so the click visibly lands.
    - **Instant disable.** `phx-disable-with` on Proceed — the button changes state the moment it's clicked, and double-submits die for free.
    - **Optimistic dismiss.** The modal hides on Proceed before the round trip (`JS.hide` alongside the submit); the maybe-write hue already shows the write in flight, and the server render reconciles. Cancel already feels instant (client `phx-click-away` aside, it's a tiny no-op event).

## 2 — Sorting

1. [x] **Sort-mode storage + inheritance.** Add nullable `sort_mode` column and non-null `sort_reverse` boolean (default false) to `tasks` and `initiatives`. `sort_mode` values: `"manual"`, `"alphabetical"`, `"completion"`, `"computed_progress"`, `"priority"`, `"created"`, `"updated"`; `nil` = inherit. `"weight"` is supported by the engine but not surfaced in the UI yet — deferred. `sort_reverse` flips each mode's natural direction. Helper `Tasks.resolve_sort/1` walks parent → root → initiative → fallback `{"manual", false}` and returns `{mode, reverse}` so direction always travels with the mode that owns it. Pure, unit-tested.
2. [x] **Sibling sort engine.** Pure function `Tasks.Sort.apply/2`: takes children + resolved mode, returns the list with new `sort_order` values. No DB. `"manual"` is a no-op. Stable sort with `id` as tiebreaker. Empty / single-element lists return unchanged. Tested standalone.
3. [x] **Sort menu in Details panel.** "Sort children by" dropdown in the Details panel for branch tasks only. Options: `Inherit` (default), criteria from .03.02.01 (no `Weight` — deferred), `Manual` at the bottom. A "Reverse" checkbox sits next to the dropdown — disabled when the resolved mode is `Inherit` or `Manual`. Selecting a criterion or toggling reverse → set `sort_mode` + `sort_reverse`, run engine (.03.02.02), persist new `sort_order`, broadcast. UI sits below the "Computed from children: X%" line. Auto-sorted parents also re-sort their children automatically whenever a child's sort-key field changes (status, computed_progress, priority, title) — `Tasks.maybe_resort_children/1` is called at every mutation site (create / update / move / recompute walk-up) so the order stays in sync with live data.
4. [x] **Resort all posterity.** "Apply to all descendants" link in the sort menu (.03.02.03). `Tasks.cascade_sort/4` walks the subtree, sets `sort_mode` + `sort_reverse` on the root and every descendant branch in one transaction, and runs the engine at each branch via `set_sort_body`. One `sort_cascaded` activity event on the root; one broadcast. Confirm modal fires when the subtree contains more than 10 branches (counts root + descendant branches); reversible only via Arc 6 undo.

5. [x] **Merge "Progress" into "Completion %".** Two near-identical sort entries confused (status buckets vs the visible %); one entry remains, labeled **Completion %**, backed by the roll-up percentage — the number on every row's underbar. Natural = least complete first; `done` sorts as 100 by construction, so done-grouping falls out for free. The status-bucket comparator is removed (its only extra — distinguishing `in_progress` — is unreachable from the UI; if `in_progress` ever becomes a real state, a bucket mode can return cheaply via the unknown-mode fallback). Stored `computed_progress` modes migrate to `completion`.

## 3 — Drag placement & refinements

1. [x] **Drag drop-band semantics.** Row-internal drop bands in `assets/js/app.js`. Center 50% → Reparent (.03.01.03 behavior). Top 25% → Sibling reorder above. Bottom 25% → Sibling reorder below. Edge bands render a placeholder in the destination slot; center keeps the row-highlight.
2. [x] **Sibling reorder mode flip.** Any reorder action (drag bands .03.03.01, root overlay .03.03.03, or keyboard `Alt+↑/↓`) sets parent `sort_mode = "manual"` and overwrites `sort_order` with the new sequence. Same transaction as the reorder; broadcast carries both.
3. [x] **Top + bottom root-level overlay drop zones.** Two slim overlay drop targets, rendered only during drag — one above the first root, one below the last. Non-root source → Promotion (lands at top or bottom of root list). Root source → root-list reorder. Hook adds them on `beginDrag`, removes on `cleanup`.
4. [x] **New-task placement defaults.** Update `Tasks.create_task/2` and `Tasks.move_task/3`:

    - Auto-sorted parent → run engine (.03.02.02); insert at sort position.
    - Manual parent, via entry form → form's slot (existing `parent_id` + sibling-position params already handle).
    - Move-in to a different parent → top of new parent (was: appended end). Auto-sort overrides.

    Tests expanded in `tasks_test.exs` and `preview_test.exs`.
5. [x] **Manual progress on branches — preserve, disable, explain.** On branches, render the `manual_progress` slider as `disabled` at the dormant value (so the preserved value is visible). Below it, muted hint: _"Ignored — this task has subtasks."_ Info icon opens a popover: _"Progress on a task with subtasks is calculated from its subtasks instead of being set manually. Your manual value is kept and will start being used again if you remove all subtasks."_ Keep the existing "Computed from children: X%" line.

    Builds a reusable `<.info_hint>` component in `CoreComponents` (popover-on-click, reuse-ready for future "explain UI semantics" uses — leaf-done snap, roll-up formula).

    Two files: `core_components.ex`, `initiative_show_live.ex` (`task_editor/1`). No `Tasks` / schema changes. Light + dark visual review.
6. [x] **Vertical-only drag — horizontal mode rolled back.** Single-mode drag: cursor picks the row under it (anchor); drop = child-of-anchor, appended. No horizontal sensitivity, no indent guide, no snap-back. Row-highlight stays. Supersedes .03.01.04, .03.01.05, and the horizontal half of .03.01.10. Sibling reorder via drag is covered by .03.03.01–.04.
7. [x] **Smooth drag highlight + drop-zone geometry.** Stable, unambiguous drop feedback with depth kept off the horizontal axis (.03.01.04–.05 stay rolled back). Refines completed .03.03.01. Touches `assets/js/app.js` (band geometry), the `task_node/1` template (tail padding), and `assets/css/app.css`.
    - **No special-casing for the progress bar.** Bands are thin strips at the row's top / bottom edges, so the whole middle — progress underbar included — falls in center; the bar is treated like any other part of the row.
    - **Center = reparent as first child** (per .03.03.04): dropping on a row's body makes the source that row's first child.
    - **Edge bands hug the row.** Thin top / bottom strips at the row's own edges; the indicator renders at the hovered row's edge, never below its subtree.
    - **Top band = sibling immediately above** this task — always available; the "before me" slot is adjacent to every row's top edge.
    - **Bottom band = sibling immediately below**, but only on rows with nothing visible beneath them (leaves and collapsed branches). An expanded branch has no bottom band — its lower area is all center.
    - **Tail padding = last child.** Every expanded branch reserves a band-height drop strip after its last child; dropping there appends the source as that branch's last child. Always present (no reflow when a drag starts), subtle when idle, highlighted during a drag. At nested tails the strips stack — one per ancestor level, innermost top → outermost bottom, each indented to its branch's child level (a leftward staircase) — and the target is chosen by vertical position.
8. [x] **Optimistic task writes, with a "saving" pink hue.** Every task write gives instant feedback, not gated on the round-trip; affected elements get a faint, mostly-transparent pink hue that clears when the DB write lands. Covers **all** write paths — reorder/sort (drag, `Alt+arrow`, sort menu / cascade), **completion toggles** (leaf checkbox + branch cascade), **attribute edits** (priority / weight / assignee via the Details form and the `P` / `W` / `A` keyboard steppers), and **deletion** (red button / `Del`).
    - **Pink exactly what the DB changes.** The hue marks only the rows / containers the write actually persists:
        - Reorder/sort don't touch progress (order-independent) → the reordered children + their `<ul>` container + the parent (whose `sort_mode` flips to manual). No ancestor pinking.
        - Completion + weight changes roll up → the toggled / edited task **and** every ancestor whose progress or status the write recomputes.
        - Priority / assignee don't roll up → just the edited row (plus its container if an auto-sort reorders siblings on the changed key).
    - **Truly optimistic (move the DOM now):** drag, `Alt+arrow` reorg, and the leaf checkbox — apply the visible change immediately, pink the affected set, let the server's re-render clear the hue (client-added class, so morphdom strips it). Revert the DOM + flash on a failed write.
    - **Pink-during-round-trip (server computes, client only hues):** sort menu / cascade, branch-cascade completion, and attribute edits — the client can't cheaply predict the result (sort criteria, cascade fan-out, rollup), so it marks the affected set "saving," then the server re-renders authoritative and the hue clears.
    - **Deletion is its own shape — remove, don't pink.** The deleted row (and its subtree) vanishes from the DOM immediately rather than turning pink; its surviving ancestor chain pinks during the round-trip while progress recomputes. On a failed write, re-insert the row and flash. This is what makes a large-subtree delete feel instant instead of waiting on the cascade + re-render.

## 4 — Task-tree layout

1. [x] **Responsive task-tree width.** Deliver the ProductSpec "Task Tree Display" standard: readable rows, scroll over squeeze, uniform roots, wrapping titles. Touches `initiative_show_live.ex` (`task_node/1` + the tree wrapper), `assets/js/app.js` (a `TreeWidth` hook), `assets/css/app.css`.
    - **Content floor + horizontal scroll.** Floor each task's content at a usable minimum width; the task-tree region (not the page chrome) scrolls horizontally when content exceeds the available width.
    - **Measured uniform width.** A `TreeWidth` hook computes one width `W = max(column width, deepest *visible* indent + floor)` for the whole initiative and applies it to the scroll-content wrapper, so every row — roots included — renders at `W` (no ragged right edge). Recompute on mount, resize, collapse/expand, and tree mutations, debounced to an animation frame.
    - **Titles wrap, never truncate.** Titles wrap within their row's `W − indent`; the attribute-chip group clips only as a last resort.
    - **Collapse shrinks the tree.** Collapsing a deep branch lowers the deepest *visible* indent, so `W` shrinks and the horizontal scroll recedes.
2. [x] **Drag edge auto-scroll.** Dragging near the top / bottom edge of the scroll area auto-scrolls it (speed ramps with depth into the edge zone) so off-screen drop targets are reachable without releasing; the drop highlight re-resolves on each scroll frame. `assets/js/app.js` only.
3. [x] **Incremental tree updates.** Restores ProductSpec § Collaboration Model (propagation work scales with the change, not the tree). Today every write and every PubSub broadcast runs `load_tree` (full reload + full re-render + full diff for every connected member).
    - **Scope the reload.** A change to task X reloads and re-renders X's affected lineage, not the whole initiative; broadcasts carry enough (task id + kind) for receivers to do the same.
    - **Keep it boring.** Prefer narrowing assigns/diffs (or LiveView streams for the root list) over inventing a patch protocol; correctness stays anchored by the existing reconcile tests.
    - **Measure before/after** with the §8-style timing pass (per-event ms on a deep tree) so the win is a number, not a feeling.

## 5 — Single-root model

1. [x] **Single root task per Initiative.** Each Initiative owns one system-managed root task; today's top-level tasks become its children. Migration: create the root task for every Initiative and reparent existing roots under it. The root task is not a tree row — selecting the Initiative selects it, and the tree renders its children. (Redefines "root" for .03.03.02–.04 and .03.05.05 — `parent_id IS NULL` is now just the root task.)
2. [x] **Sort moves onto the root task.** `resolve_sort` terminates at the root task; drop `sort_mode` / `sort_reverse` from the `initiatives` table. (Redoes part of completed .03.02.01 — sort storage now lives only on tasks.)
3. [x] **Initiative subtitle via the root task's `title`.** Optional subtitle stored in `root.title`; a system-managed write path stores `""` when empty (column stays non-null; normal task-title `validate_required` + min-1 untouched). No new `initiatives` column.
4. [x] **Initiative Details = root-task controls + Initiative-only fields.** Surface the root task's sort menu (.03.02.03: "Sort lists by" + Reverse + "Apply to all") and rolled-up progress (the computed-from-children line) in the Initiative pane, merged with name / subtitle / description / delete; no field shown twice. Sort menu extracted to a shared `<.sort_menu>` component reused by the Task pane. Members stay in their own panel (not folded in). Reuses `set_sort` / `cascade_sort` on the root task — no separate Initiative-sort backend.
    1. [x] **Show/Hide Members toggle (mobile).** A button under the progress bar expands a collapsible members section inline and flips between "Show Members" / "Hide Members" — plain client-side show/hide. Mobile only; desktop keeps the standalone Members panel (one members UI per breakpoint, nothing shown twice).
    2. [x] **New List + Show Members share a row** under the progress bar on mobile, centered. (Desktop right-anchors New List in the title row.)
5. [x] **Promotion + root-zone drops under the single root.** "Promote to root" = make a child of the Initiative's root task; the top/bottom overlay zones and new-task placement target that child list. (Redoes the root-targeting behavior of completed .03.03.02–.04.)

## 6 — Initiative list

Iterative polish of the Initiatives index (the dashboard list of initiatives).
Worked by the seat of the pants in-browser, like worklist 4; items get firmed up
as they're discovered rather than fully specced up front.

1. [x] **Mobile initiative-card layout.** Give the card room on phone. (Description unchanged — keeps its 2-line clamp + ellipsis.)
    1. [x] Move "Your role" + the updated timestamp off the title row onto their own row.
    2. [x] Add a subtitle row (`root.title`) before the description, shown only when a subtitle exists.
2. [x] **Initiative-list sort order.** A sort menu like the task sort (criteria + Reverse, reverse remembered per mode) for the Initiatives index. Stored in **localStorage for now** — inherently per-user (per-browser), no schema change. The sort mode + reverse and the manual order all live client-side; recapture as proper per-user server preferences when user profile/preferences is built (BACKLOG). Caveat: per-browser, so it won't follow a user across devices until then.
3. [x] **Drag-to-reorder initiatives.** The card's grove icon (flanked by `⋮ … ⋮`) becomes the drag handle. Edge bands insert the card before/after another and persist the new order to localStorage (per item 2). **No reparent / center drop** — dropping onto another Initiative isn't a feature here, so there's no center "child-of" zone, only the before/after banding.
4. [x] **New Initiative button.**
    1. [x] Style it like the "New List" button (emerald-bordered).
    2. [x] On mobile, give it its own row after the "Your Initiatives" heading and its subsequent copy.
5. [x] **Add the progress bar as the bottom row on initiative cards.**

## 7 — Details Pane & Keyboard shortcuts

1. [x] **Sticky, self-scrolling Details pane.** Make the Details pane stick to the top of the `<main>` element regardless of main's scroll position, with a max-height so its own contents scroll when there isn't enough vertical room for them.
2. [x] **Keyboard shortcuts.** Single-key accelerators for task navigation + actions. **A keyboard shortcut never fires while an element that accepts text input is focused** (input, textarea, `select`, contenteditable) — it falls through. With **no task selected, only `Enter` responds**; every other shortcut needs a selected task. Where a shortcut is a letter on a labeled button, that letter is underlined on the button (e.g. `N` on New Subtask); non-letter keys (Enter, Space, arrows) live only in the help surface. (New keys arrive as added subitems.)
    1. [x] **Shortcuts help surface (data-driven).** One `{keys, label}` list drives a `?`-triggered overlay listing every shortcut — the discovery home and the only home for the non-letter keys. New shortcuts are a one-line addition. A small "⌨ shortcuts" affordance opens it too, placed at the **bottom of the Initiatives list, desktop only** (hidden on mobile, which won't use keyboard shortcuts).
    2. [x] **`Enter` — toggle selection / pane.** No task selected → reselect the last-selected task and reopen its pane (fallback to the first task if there's no history). A task selected → deselect and hide the pane. Requires remembering the last-selected task across a close.
    3. [x] **`Space` — expand / collapse the selected task's children.**
    4. [x] **Plain `↑/↓/←/→` — move the selection.** Navigate which task is selected, no reorg. `↑/↓` = previous / next visible task; `←` = select parent, `→` = select first child.
    5. [x] **`Alt+↑/↓/←/→` — reorg (fix).** Keep the Alt modifier for sibling-reorder / dedent-indent (.03.01.06); fix it currently not firing.
    6. [x] **`N` — New Subtask of the selected task.** Same as clicking that task's New Subtask / New Task button; underline the `N`.
    7. [x] **Keyboard selection scrolls into view.** When the keyboard moves or restores the selection, scroll only if needed so the selected task's **row** (not its subtree) comes into view — `scrollIntoView({block: "nearest"})`.
    8. [x] **`S` — New Sibling of the selected task.** Parallel to `N`; underline the `S` in "Add Sibling".
    9. [x] **`P` / `W` / `A` — adjust priority / weight / assignee of the selected task.** `P` steps priority up (clamped: `high` + `P` stays `high`), Shift+`P` down. `W` ±1 (no wrap; Shift+`W` floors at 1). `A` cycles assignee with wrap, including **Unassigned** as a stop; Shift+`A` back. Underline `P`/`W`/`A` in the Details-pane labels.
    10. [x] **`Alt+letter` — focus that field.** `Alt+P` / `Alt+W` / `Alt+A` (and any future labeled input) focus the input for precise editing, via the existing `push_focus` / `FocusField` plumbing. The pane is always open when a task is selected, so the fields are present.
    11. [x] **`Del` — delete the selected task.** Fires the same confirmed delete as the pane's red Delete button (clicks it, so the confirm dialog still gates it). A non-letter key, so it lives in the help surface only.
    12. [x] **Bonk sound on impossible reorg moves.** `Alt+↑` on a first child, `Alt+↓` on a last child, `Alt+←` on a top-level task, `Alt+→` with no previous sibling: play a short low "bonk" (WebAudio, no asset), skip the server round-trip, and don't pink anything.
3. [x] **Client-first selection.** Restores UX_GUARDRAILS 6.5 — selection is view state but lives as a server assign threaded through every `task_node`, so each arrow press re-diffs the whole tree behind a 4–6-query round trip.
    - **Instant highlight.** Selection highlight + arrow-key movement happen client-side (class swap, like collapse); the row never waits on the network.
    - **Pane follows.** The server still gets a `select_task` event, but it only loads and re-renders the Details pane — `selected_id` stops being a per-row render input (row highlight comes from a client class, `data-selected` maintained by the hook).
    - **Keyboard moves keep working.** `.TaskKeys` reads selection from the DOM already; reorg / adjust / delete paths keep their semantics, now without the per-keystroke tree diff.
    - **Guard the seams.** Selection must survive re-renders/broadcasts (same observer-net pattern as collapse) and stay consistent when the selected task is deleted or moved by another user (pane closes / refreshes via the existing PubSub handlers).

4. [x] **Instant Details pane** (UX_GUARDRAILS 6.1 + 6.5, extends .03.07.03). Selecting a task opens the pane shell immediately — the **real pane UI**, not placeholder rectangles: fields whose values already live in the row (title, priority, weight, assignee — the pills) prefill live; genuinely-unknown sections (description, comments, activity) are disabled "Loading…". The server render replaces the shell. Client-side and transient (a timeout guards against a stuck shell); closing via the pane's X / backdrop clears the client selection too. Mobile's flyout keeps server timing for now (its overlay layout classes are server-keyed).

5. [x] **Initiative pane doubles as the Settings panel.** The initiative editor gains a second headed section, **Settings**, for initiative-wide behavior — currently "Sort lists by" and the (admin-only) Delete as the danger tail. Future per-initiative settings (e.g. the progress calc mode, BACKLOG → Initiative settings) land here.

6. [x] **One pane, never swapped** (reworks .03.07.04 — the shell/real swap was UI jerk-around). The real pane stays mounted once created: deselecting hides it (`selected_task` keeps the last pane data; only `selected_task_id` nils), and on a selection switch the client writes the row-known values (title / priority / weight / assignee) into the real fields immediately while the `[data-pane-async]` sections (comments, activity) dim; the server patch reconciles the same elements in place. LiveView never patches the focused element, so in-progress typing survives. The only server-timed open left is the very first selection of a page visit.
7. [x] **Initiative Settings, collapsed** (reworks .03.07.05). "Sort lists by" returns to the details body; **Settings** becomes a native `<details>` collapsed by default (a `KeepOpen` hook preserves its state across patches), holding the per-initiative progress-calc selector (.03.09.02). Delete stays at the pane's bottom as before.

8. [x] **No layout shift in the panes; instant initiative editor** (UX_GUARDRAILS 1.1 + 6.5). The branch-only copy ("Ignored — this task has subtasks.", "Computed from children: N%") and the sort menu keep their space when not applicable (`invisible`, not removed), so leaf↔branch selection switches never shift the layout. The initiative editor gets the persistent-pane treatment: always rendered (reads `@root_task`, kept fresh by load_tree/patch), `hidden` toggled — the title click swaps panes client-instantly. Sort controls carry their own task id (hidden `task_id` input / `phx-value-id`), so the editor no longer hijacks the task selection. The calc selector gains a "?" info hint explaining both methods.

9. [x] **Zero-jank pane, second pass** (UX_GUARDRAILS 1.1 + 6.2). (a) `scrollbar-gutter: stable` on the pane container — the scrollbar's lane is permanent, so content stops reflowing sideways when it appears. (b) The sort UI joins the client-instant fields: each task `<li>` carries its own `sort_mode` + `reverse` as data attributes, and the in-flight fill flips the sort block's visibility (children-presence from the DOM) and sets the select / Reverse checkbox; the Inherit parenthetical corrects on the patch, text-only. (c) Comments + activity swap to an explicit "Loading…" while in flight (never another task's data); the add-comment form stays live — `select_task` enters the event queue first, so an immediate comment lands on the new task. (d) `info_hint` rebuilt on the native Popover API: top-layer rendering escapes the pane's overflow (it used to get clipped and force scrollbars), viewport-clamped next to its trigger, light-dismiss built in.

10. [x] **No view-state server hits left** (UX_GUARDRAILS 6.5 sweep, operator mandate). Opening/closing the add-task forms phoned home before a single character could be typed; so did the Add Member form. Now: ONE add-task form parked in a `phx-update="ignore"` home, client-teleported into per-row ignored slots (subtask, sibling-after, root) by buttons / keyboard `N`+`S` — no patch can disturb it mid-typing; `create_task` reads `parent_id`/`after_id` from the form itself; submit clears the title and keeps focus for rapid entry. Add Member is a client-toggled `<details>` (KeepOpen). The `show_add_*` / `cancel_add` / `kbd_new_*` / `show_member_form` server events are gone. Remaining server hits from the UI are all data: pane content load, writes, and confirm flows.

11. [x] **Optimistic row echo for pane edits** (UX_GUARDRAILS 6.2, operator directive). A pane field edit already states exactly what the row will show, so the client writes it at the keystroke: title, description (row line always in the DOM, hidden when empty), the three pills, and a leaf's progress bar + the pane's % readout. Pills carry a `data-pill-set` state attribute styled by Tailwind data-variants, so the echo flips one attribute instead of juggling class lists. Roll-up math (ancestors, branch %) is never predicted — the saving hue covers it; the server patch re-renders the same values, and a rejected write restores the truth.

12. [x] **Collapsed badge counts subtree leaves** (operator directive). The chevron's collapsed "(N)" shows the leaf tasks folded inside the whole subtree, not the direct-child count — how much work is hidden, not how many branches wrap it.

13. [x] **Optimistic leaf checkbox flip** (operator judgment in §8.18.1). The completion checkbox's styling keys off `aria-pressed` (check icon included), so a leaf toggle flips it client-side in one attribute write. Leaf toggles commit directly — no confirm class covers them, so nothing can intercept the flip. Branch toggles stay server-timed: they may raise a confirm, and their flip belongs to the cascade's outcome.

14. [x] **Confirms preserve optimism** (UX_GUARDRAILS 6.6, operator finding in §8.20). A drag that raises a completion-flip confirm used to snap the row back while the modal opened. Now the optimistic placement HOLDS while the user decides: the client parks a revert handle, and the server announces the outcome — `confirm-cancelled` (Cancel, click-away, or a failed Proceed) sends the row home; `confirm-resolved` (Proceed) releases the handle to the commit's re-render.

15. [x] **Client-instant delete confirm** (UX_GUARDRAILS 6.5/6.6, operator finding). Pressing `Del` / clicking Delete waited a round trip before the dialog appeared — but everything the dialog shows (the task title, the irreversibility copy) is already in the DOM. The confirm is now a client-rendered modal (`#delete-confirm`, `phx-update="ignore"`): opens at the click with the title prefilled, holds a sticky maybe-write hue on the doomed subtree, Cancel/Escape/backdrop close it free, and only Delete touches the server (`delete_task` event + the optimistic row removal). The server-side `request_delete_task`/`:delete_task` pending plumbing is gone; this also fixes a latent race where an in-flight pane switch could have deleted the previously selected task. The initiative-delete confirm still round-trips — same candidate treatment if it ever grates.

16. [x] **Client-instant Cancel on server confirms** (UX_GUARDRAILS 6.5/6.6, operator finding). Cancel must let the user hop to their next action with zero wait — but the held drag's trip home and the maybe-write unpinking sat on the `cancel_pending` round trip. Now the Cancel button / backdrop click reverts the held placement and strips the hue at the click; the server event reconciles the same state behind it (and stays as the backstop for the failed-Proceed path).

17. [x] **Field focus never round-trips** (UX_GUARDRAILS 6.5 sweep, operator mandate — full audit of every server event). `Alt+P/W/A` and pill taps pushed `kbd_focus_field` / a `focus` param just to focus a pane field that already exists client-side. Both focus directly now (pills carry `data-pill` naming their field); a pill tap on a first-ever selection parks the focus in `DoitPendingFocus` and the pane-arrival sync lands it. The `kbd_focus_field` event, the `focus` param, `push_focus`, and the `FocusField` hook are gone.

18. [x] **Client-instant initiative-delete confirm** (same sweep; the .03.07.15 sibling). `request_delete_initiative` round-tripped to show a dialog whose content is known at render. Now a client modal (`#delete-initiative-confirm`, name server-rendered into it); Cancel/Escape/backdrop are free; Proceed pushes `delete_initiative`. With it the last `:delete_*` pending kinds are gone — the server confirm modal is now only ever non-destructive (Proceed/emerald), so its destructive styling paths are deleted.

19. [x] **Initiatives index joins UX_GUARDRAILS 6.5** (same sweep). (a) "New Initiative" phoned home before the name could be typed (`show_new`/`cancel_new`) — now a client-toggled `<details>` + KeepOpen; only a successful create touches the form from the server (`close-details` push event closes + resets it; a validation error re-render leaves it open). (b) Sort-control changes waited on the server re-stream to reorder rows whose keys the client can see — cards now carry `data-name/-progress/-created/-updated` and explicit modes reorder client-side at the change; the re-stream confirms. "Recent" (the default, owner-first server order) stays server-derived.

20. [x] **Mobile flyout opens at the tap** (same sweep; closes .03.07.04's "mobile keeps server timing" remainder). The rail's open/closed state rides one `data-open` attribute on `#details-rail` — all class differences (overlay vs sticky vs hidden) hang off it as Tailwind data-variants, and the backdrop is always rendered with `hidden` toggled. The client flips both at the tap (`syncRail`, wired into the selection/pane sync); the server patch confirms the same attribute.

21. [x] **Manual-progress writes pink their roll-up scope** (operator finding in §8.18.3). The slider's write was missing from the saving-hue wiring entirely — no pink on the row or the ancestors whose computed progress it moves. Now pinks row + ancestors, same as weight. The ancestor roll-up VALUES stay server-computed (the hue covers the in-flight window): mirroring leaf-average/single-level math with path-product weights in JS would double the math surface the Engineering Style deliberately keeps in one pure, unit-tested module.

22. [x] **Fully-optimistic operated row** (operator directive: "few things are more annoying than wondering whether a checkbox is working"). Any completion toggle — leaf or branch — flips the operated row at the click: checkbox (`aria-pressed`), done styling (`data-done` on the row drives the strikethrough + bar color via group-data variants), and the row's bar (done → 100; reopened leaf → its dormant manual value via `data-manual-progress`; reopened branch waits for the recompute). The push replies like `move_task`: `ok: false` reverts, `committed: true` releases, `committed: false` (cascade confirm open) HOLDS the flip through the modal via the guard net (6.6) — Cancel reverts it, Proceed keeps it. Ancestor/descendant rows still arrive with the patch; their window is the pink.

23. [x] **Uncheck → 0, and indeterminate bars for in-flight %s** (operator findings + design). Two corrections to .22: (a) the "dormant manual value" restore was a no-op — `maybe_set_done_progress` sets `manual_progress` to 100 on every →done and 0 on every done→open transition (cascades included), so EVERY toggle outcome is knowable: 100 on check, 0 on uncheck, leaf and branch alike; the operated row's bar now flips both directions and `data-manual-progress` is gone. (b) Rows whose % genuinely IS in flight — ancestors of progress-affecting writes (toggles, weight, slider, reparents), cascade descendants, and flip rows held by a confirm — stop claiming a number (operator design): `is-recomputing` turns the bar into a full-width left-to-right fade from the fill color into the track (no fill-edge to misread, dark mode free), pulsing, with "…" as the readout (CSS-only text swap). Client-added alongside the pink and cleared with it; server-rendered for the held-modal era via `pending_recompute_ids` (everything in `pending_saving_ids` except the operated task; sort-only confirms excluded).

## 8 — Testing

Verification, in execution order.

> **Judging the saving hue (pink / "flash"):** on a local stack, writes confirm
> in tens of milliseconds — the optimistic hue lives a frame or two and is
> genuinely invisible. To see and judge any flash check, run
> `liveSocket.enableLatencySim(500)` in the browser console first (persists for
> the session; `liveSocket.disableLatencySim()` ends it). The hues that persist
> without it are the server-held ones: maybe-write pink while a confirm modal
> is open.

1. [x] **[Subagent] Unit tests for `Tasks.preview_create/2` and `Tasks.preview_move/3`.** Each scenario (1 / 2 / 3 / nil) plus the body-error path.
2. [x] **[Subagent] Reconcile-correctness tests** for the `body + reconcile_after_*` split — both old and new parent chains reconcile and broadcast correctly when moving across lists; no double-broadcast; no missed ancestor.
3. [x] **[Agent] `mix precommit` clean** — warnings-as-errors pass, format clean, full suite green.
4. [x] **[Subagent] Sort backend tests** (.03.02.01, .03.02.02). Unit tests for `Tasks.resolve_sort_mode/1` cascade (parent → root → initiative → fallback) and `Tasks.Sort.apply/2` across each criterion + manual no-op + stable-sort tiebreaker.
5. [x] **[Subagent] Mode-flip + placement backend tests** (.03.03.02, .03.03.04). New-task placement across the three cases (auto-sorted, manual via form, move-in); reorder action sets parent `sort_mode = "manual"` and overwrites `sort_order`.
6. [x] **[Human] First in-browser pass on drag modes.**
    1. [x] **Vertical reparent.** Pick any leaf; drag onto a different row at any depth; release. → Leaf becomes a child of that row; underbars on both old and new parent chains update.
    2. [x] **Vertical no-op — drop on self.** Pick any task; hover its own row; release. → No commit, no flash.
    3. [x] **Vertical no-op — drop on descendant.** Pick a branch with at least one child; hover one of its descendants; release. → Drop rejected; no commit.
    4. [x] **Desktop cross-list drag** (.03.01.07). Drag a task in one List onto a task in another List of the same Initiative; release. → Task moves; both Lists' underbars update.
    5. [x] **Horizontal cursor over source row is a no-op** (.03.03.06). Drag, then move horizontally only (no vertical travel) over the source row; release. → No commit. Confirms horizontal mode is gone.
7. [x] ~~**[Agent] Playwright browser-test rig.**~~ Adopt `phoenix_test_playwright` (ExUnit + Ecto-sandbox tests driving headless Chromium) so the logic-heavy browser checks (completion confirms, keyboard, suppression) run by machine. Sort-menu, drag-band, touch, and visual passes stay [Human]. Runs once or twice per arc, never in the routine dev loop. _Retired — rig and suites deleted (operator call: hand-testing is faster than the rig ever was); browser verification is [Human] action items from here on._
    1. [x] **[Agent] Infra.** Add the dep; run the browser in a `playwright` compose service (the Alpine web image can't host Playwright's glibc Chromium — tests connect over websocket); tag e2e tests so the plain suite stays fast (`mix test` excludes them, `mix test.e2e` includes them); README run commands.
    2. [x] **[Agent] Smoke test.** Render an initiative in the real browser, select a task, toggle a leaf checkbox — proves the rig before the suites pile on. (Its first run caught a real bug: `aria-pressed` rendered as a presence-only attribute — invalid ARIA.)
    3. [x] **[Agent] Completion-confirm e2e** (machine half of item 8). Scenarios 1 / 2 / 3 plus the Cancel path, and second-tab broadcast via two browser contexts.
    4. [x] **[Agent] Keyboard e2e** (item 9, .03.07.02). Real keydowns through `.TaskKeys`: selection + arrows, `Enter` / `Space`, `N` / `S`, `P` / `W` / `A` stepping, `Alt+P` focus, `Del`, scroll-into-view, and suppression with focus in a text field.
    5. [x] **[Agent] Confirmation-suppression e2e** (.03.01.11). "Don't show this again" persists across reload via localStorage; a suppressed class commits modal-free; other classes keep prompting; deletes always confirm; `accept_dialogs: false` fails the run on any native dialog.
    6. [x] **[Agent] Trim absorbed checks** from the keyboard-shortcuts and suppression items, leaving visual-only remainders (since folded into the action passes below); sort-menu / drag / touch / visual passes stay [Human] in full.
8. [x] **[Human] Completion-confirm modal in browser.**
    1. [x] **Scenario 1 (would uncomplete).** Set up: an ancestor marked `done` with all its descendants also `done`. Drop an incomplete leaf under it (or create one). → Modal appears listing the ancestor; click Proceed; ancestor's check clears; the change broadcasts to a second open tab.
    2. [x] **Scenario 2 (would complete).** Set up: an ancestor with exactly one remaining incomplete descendant. Mark that descendant done via its row checkbox. → Modal appears listing the ancestor; click Proceed; ancestor checks; broadcasts to a second tab.
    3. [x] **Scenario 3 (both directions).** Move the last incomplete child of a source branch into a `done` destination branch whose descendants are all `done`. → Modal lists both kinds of flips; Proceed commits both correctly.
    4. [x] **Cancel path.** From any scenario above, click Cancel (or click the backdrop) instead of Proceed. → Tree state matches pre-action; no events in the activity log; no flash.
    5. [x] **Light + dark visual review** of the modal — readable text, button contrast, focus ring on Proceed and Cancel, scrollable affected-tasks list when the list is long.
9. [x] **[Human] Keyboard reorganization path** (.03.01.06).
    1. [x] **Alt+↑** moves the selected task up among its siblings; selection persists (Details panel stays on the same task).
    2. [x] **Alt+↓** moves the selected task down among its siblings; selection persists.
    3. [x] **Alt+→** indents the selected task under its previous sibling; selection persists.
    4. [x] **Alt+←** dedents the selected task (becomes a sibling of its current parent); selection persists.
    5. [x] **Tab-order intact** after any of the above — Tab moves through interactive elements in expected order; no orphaned focus, no skipped elements.
    6. [x] **Suppression in text inputs.** With focus inside a title-edit input, description editor, or any contenteditable, Alt+arrow keys do NOT trigger reorganization — they fall through to native field behavior.
    7. [x] **Bonk on impossible moves** (.03.07.02.12). Each of the four boundary cases plays the bonk; nothing pinks, nothing moves, no event in the activity log. `Alt+↑/↓` on a legal move pinks the two swapped rows, plus the parent only when the reorder flips it to manual sort — never higher ancestors.
10. [x] **[Human] Mobile long-press** (.03.01.08) on a real touch device.
    1. [x] **Long-press starts drag.** Press and hold the drag handle ~400 ms; drag preview appears; release moves the task.
    2. [x] **Short tap is a no-op.** Quick tap on the drag handle (no hold) → no drag, no selection change.
    3. [x] **Scroll-vs-drag tolerance.** Press the handle then swipe vertically before 400 ms → page scrolls; no drag starts.
    4. [x] **Cross-list mobile drag.** Long-press a task in one List, drag onto another List within the same Initiative, release. → Task moves; underbars update.
11. [x] **[Human] Sort-mode pass** (.03.02.03, .03.03.08). On a branch with ~5 child branches and mixed titles: pick **Alphabetical**, toggle **Reverse**, reload, pick **Manual**, pick **Inherit**.
    1. [x] Criterion pick: children resort; menu shows the explicit mode.
    2. [x] The resorted children flash the saving hue during the round-trip.
    3. [x] Reverse: opposite order; checkbox survives the reload; disabled on Inherit / Manual.
    4. [x] Manual: no resort; `sort_order` unchanged.
    5. [x] Inherit: the option names what's being inherited with direction folded into the wording — "Inherit (Alphabetical A–Z)", "Inherit (least complete)", "Inherit (Manual)" when nothing above sets a mode — and the children resort to that inherited order immediately.
12. [x] **[Human] Cascade-sort pass** (.03.02.04, .03.01.11). **Make descendants inherit** on a small (~5-branch) subtree; then on a >10-branch subtree — Cancel first, redo and Proceed.
    1. [x] Small cascade: every descendant branch flips to Inherit (menu reads "Inherit (…)") and re-orders to follow this branch — a live link, not a stamped copy.
    2. [x] Large cascade: the confirm modal names the switch to Inherit; the subtree holds the maybe-write hue while it's open; Cancel = no-op and unpinks; Proceed commits.
13. [x] **[Human] Row-edge drag pass** (.03.03.01, .03.03.08). Drag a task onto a sibling's top edge band, then another onto a bottom edge band.
    1. [x] Top edge → reorder above; placeholder visible during the drag.
    2. [x] Bottom edge → reorder below.
    3. [x] Each drop: the row moves optimistically; moved row + affected siblings flash; the parent only when the reorder flips it to manual sort; never higher ancestors.
14. [x] **[Human] Root-overlay drag pass** (.03.03.03). Drag a non-root task into the top root overlay, another into the bottom one; then a root task into each.
    1. [x] Non-root → top / bottom overlay = promotion to that end of the root list.
    2. [x] Root → top / bottom overlay = root-list reorder to that end.
    3. [x] Any cross-parent drop pinks the moved row + **both immediate parents** (their % moves); ancestor chains above stay quiet.
15. [x] **[Human] Responsive task-tree width + edge auto-scroll** (.03.04.01, .03.04.02).
    1. [x] **Deep tree scrolls; roots uniform.** Expand a tree deep enough to exceed the viewport. → Tree scrolls horizontally; every top-level row shares one width (no ragged right edge).
    2. [x] **Collapse shrinks the tree.** Collapse the deepest branch. → Scroll width shrinks; the scrollbar recedes once it fits.
    3. [x] **Narrow viewport floors at the minimum.** Shrink the window below the floor. → Rows hold the minimum width and the tree scrolls rather than crushing content.
    4. [x] **Long title wraps, never truncates** within its row's width — and a single long title does not widen the whole tree.
    5. [x] **Drag to an off-screen target.** Start a drag and hold near the bottom edge. → Tree auto-scrolls; the drop highlight tracks the row under the pointer; release drops where expected.
16. [x] **[Human] Keyboard-reorder pass** (.03.07.02, .03.03.08, .03.07.03). Select a mid-tree task; move the highlight with plain arrows, then Alt+↑ and Alt+↓.
    1. [x] Plain ↑/↓/←/→ move the highlight instantly — no network wait; the pane shell opens at once with the title (skeleton below), and the fields fill when the server responds.
    2. [x] The two swapped rows flash (parent only on a manual-sort flip); never higher ancestors.
    3. [x] Judge whether the row visibly lags the pink → wants a true optimistic move — or already feels instant.
17. [x] **[Human] Attribute-stepper pass** (.03.07.02, .03.03.08). With a mid-tree task selected, step P / W / A (with Shift variants).
    1. [x] The selection's shortcut underlines show on that task only: `N` on its New Subtask button, `S` on Add Sibling, `P` / `W` / `A` in the Details labels.
    2. [x] P / A steps pink only the edited row; W pinks the row + ancestors (weight rolls up).
18. [x] **[Human] Leaf-toggle pass** (.03.03.08). Check an open leaf, then uncheck it.
    1. [x] The toggled row + ancestor chain show the saving hue (pink); judge whether the checkbox flips fast enough, or wants an optimistic flip. Judgment: wants optimism → built as .03.07.13.
    2. [x] The saving hue (pink) reads correctly in dark mode too (spot-check).
    3. [x] The checkbox flips at the click — no server wait, no flicker when the patch confirms it (.03.07.13).
19. [x] **[Human] Branch-cascade pass** (.03.01.11, .03.03.08, .03.01.12). Cascade-complete an open branch via its checkbox — Cancel once, redo and Proceed; reopen it the same way.
    1. [x] While the modal is open the subtree + ancestors hold the maybe-write hue; Cancel unpinks everything.
    2. [x] Proceed keeps the hue through the write; both directions cascade correctly.
    3. [x] Buttons acknowledge the click: Proceed / Cancel visibly press, and the modal dismisses instantly — the write's result arrives via the hue, never a frozen dialog.
20. [x] **[Human] Flip-move pass.** Setup: branch A with all children done except one; branch P fully done. Drag A's incomplete child onto P's row.
    1. [x] A confirm opens naming A and P. The dragged row sits where you dropped it (under P). That row, A, and P are pink.
    2. [x] Click Cancel. Instantly: modal gone, row back under A, no pink. A still open, P still done.
    3. [x] Drag it to P again; click Proceed. The row never moves again; A's checkbox checks, P's unchecks; pink clears when the write lands.
21. _**[Human] Delete pass** moved to §11.1._
22. [x] **[Human] Branch progress-control pass** (.03.03.05). Select a deep branch and read its Details pane, in light and dark.
    1. [x] The progress slider renders disabled at the dormant `manual_progress` value, with the muted hint _"Ignored — this task has subtasks."_
    2. [x] The info popover opens from the icon; clicking outside dismisses it.
23. [x] **[Human] Sticky-pane pass** (.03.07.01). With a long tree and a task's Details open, scroll `<main>` top to bottom, in light and dark.
    1. [x] The pane stays pinned to the top of the visible area; the page never pushes it away.
    2. [x] When the pane's own contents exceed the height, they scroll inside it.
24. [x] **[Agent] Leaf-average roll-up tests** (.03.09.01). Unit tests pinning the design examples (nested branch dilution, equal-leaf agreement case), path-product weights on intermediate branches, done-leaf snap, zero-weight subtree exclusion, and compute_all parity with compute.
25. [x] **[Agent] Calc-setting + pane-unification tests.** Single-level average mode unit-pinned against the leaf-average examples (25 vs 33, 75 vs 86); reconcile honors the per-initiative setting end to end (20 → 50 on switch).
26. [x] **[Human] Round-trip-audit pass** (.03.07.17–22). The client-instant surfaces from the audit, each judged by feel — no wait, no flicker when the server confirms.
    1. [x] Pill taps (P / W / A chips) focus their Details field at the click — including on a task that wasn't selected yet (focus lands when the pane fills). `Alt+P/W/A` focus instantly.
    2. [x] Initiative pane → Delete initiative: dialog opens instantly; Cancel / Escape / backdrop close free; Proceed deletes and redirects.
    3. [x] Initiatives index: "New Initiative" opens the form at the click with typing immediately possible; Cancel closes free; Create closes + clears it; a failed create (blank name) keeps it open with errors.
    4. [x] Initiatives index: changing Sort / Reverse reorders the cards at the change (except "Recent", which may take a beat).
    5. [x] Phone width: tapping a task slides the details flyout in instantly; the backdrop tap and ✕ close it instantly.
    6. [x] Any checkbox — leaf or branch — flips at the click, with the strikethrough and the row's bar following instantly: 100 on check, 0 on uncheck, both directions, both kinds (.03.07.23). No flicker when the patch lands.
    7. [x] A confirm-gated branch cascade: the branch's checkbox flips at the click and HOLDS while the modal is open; Cancel snaps it (and its styling) back; Proceed keeps it.
    8. [x] Under latency sim: dragging the Manual progress slider pinks the row + its ancestors while the write is in flight.
    9. [x] Under latency sim: rows whose % is in flight — ancestors of a toggle/weight/slider write, cascade descendants, flip rows while a confirm decides — show the indeterminate bar: full-width emerald→track fade, slow pulse, "…" instead of a number. The operated row never does (its bar shows the real new value).

## 9 — Progress roll-up

1. [x] **Leaf-average roll-up (AbstractSpoon-style default).** A branch's progress is the weighted average over **all descendant leaves** — every leaf counts wherever it sits — replacing the single-level average (each direct child one unit). A leaf's effective weight is the product of the weights along its path, so weighting an intermediate branch scales its whole subtree; all-default weights = a plain leaf average. Reconciliation recomputes the initiative from one query and writes only diffs (leaf averages aren't derivable level-by-level from persisted child percentages). The Initiative header bar becomes the system root's roll-up — same math end to end. ProductSpec § Roll-up Progress is the canonical formula.
2. [x] **Per-initiative progress-calc setting.** `initiatives.progress_calc` (`"leaf_average"` default, `"single_level"`), chosen from the Initiative pane's collapsed Settings (.03.07.07). Switching recomputes the whole initiative under the new mode and broadcasts a full-reload to members. `Progress.compute/2` + `compute_all/2` carry both modes.

## 10 — Weight removal

Remove the per-task Weight attribute. Decomposition is the weighting mechanism: leaf-average roll-up makes a subtree's pull equal its leaf count, the AbstractSpoon inspiration has no weight either, and weight-sort was already built-but-never-surfaced. ProductSpec § Durable Principles carries the rationale.

1. [x] **Math: unweighted roll-up.** `Progress` drops path-weight products — leaf average becomes `sum(leaf_progress) / leaf_count`; single-level mode becomes a plain average of direct children. Zero/non-positive-weight exclusion behavior and its tests go away.
2. [x] **UI: weight surfaces out.** Remove the weight pill, the Details-pane Weight field, and the `W` / `Shift+W` stepper (key freed; pane label underline goes with it). Weight wiring in the saving-hue/recompute paths goes too (ancestor pinking for the other progress writes is untouched).
3. [x] **Sort engine: drop the `"weight"` mode.** Turned out to already be true — the engine never carried a weight comparator; only a comment claimed deferred support. Comment removed.
4. [x] **Schema: drop `tasks.weight`.** Migration removes the column; changeset casts, `validate_weight`, and the seed default go with it. Activity log stops listing weight as a tracked field.
5. [x] **Doc sweep.** On approval: remove the draft banners here and in ProductSpec; update CLAUDE.md (core idea, vocabulary, principles), README (pitch, vocabulary, formula, activity-log field list), and the `info_hint` future-uses mention in .03.04; CHANGELOG entry.
6. [x] **Tests.** Update progress/sort/context/LiveView tests for the unweighted math and removed surfaces; full suite green.

## 11 — Final testing

The unfinished verification, gathered from the passes above as the arc closes.

1. [x] **[Human] Delete pass** (.03.01.11, .03.03.08, .03.07.15). Select a branch with children, press `Del` — Cancel once, then `Del` → Proceed.
    1. [x] The dialog opens instantly at `Del` — no server wait — prefilled with the task's title.
    2. [x] While it's open the doomed subtree holds the maybe-write hue; Cancel (or Escape / backdrop click) unpinks.
    3. [x] Proceed: the row + subtree disappear immediately; ancestors flash while progress recomputes.
2. [x] **[Human] Weight-removal eye-check** (.03.10). No Weight anywhere — row pills, Details pane, keyboard (`W` does nothing); bars and roll-ups behave unchanged.
3. [ ] **[Human] Interleaving pass.** Operations colliding mid-flight — the one surface no single pass combines. Scale and mixed sort modes are already covered by the operator's real trees (28 tasks at depth 19, manual/alphabetical/completion mixed). The operator plays both actors: two windows on the same Initiative, one hand on the keyboard (focused window), one on the mouse (hovering the other) — near-simultaneous writes; for collisions that need a sustained gesture, hold the drag and Alt+Tab the keyboard to the second window.
    1. [x] Mid-move collision: a keyboard move (`Alt+←`) under a completion-sorted parent fires together with a checkbox click on a leaf there from the second window → both writes land, the resort settles cleanly around the moved row, no snap-back, both windows agree.
    2. [x] Held-confirm collision: with a flip-drag's modal open, the second actor writes elsewhere in the tree → the held placement and maybe-write hue survive the patch; Cancel/Proceed still resolve correctly.
    3. [ ] Cascade vs. reopen: cascade-complete a branch while the second actor reopens a leaf inside it → final state is consistent in both windows; no stuck pink.
    4. [ ] Edit vs. move: the second actor retitles a task while the operator drags that same task cross-list → both changes land; pane and row agree.
    5. [ ] Same-task field race: both actors edit different fields of one task back-to-back → last write per field wins; no field silently reverts.

## Cross-arc note

The drag infrastructure picked here (library, hook abstraction, motion conventions, drop-feedback visuals) needs to be **reusable for Arc 5 item 11** — drag-a-person from the cross-Initiative people pane onto an Initiative entry in the left rail. Same primitives, different source / target. Pick something that can host both gestures without forking; favor a small custom hook over a heavy library if the heavy library only fits one of the two cases.

## Implementation Notes

- **Library vs. roll-your-own.** Custom `phx-hook` over pointer events. Sortable.js wrappers don't extend cleanly to the cross-pane Arc 5 item 11 case; one small custom hook serves both gestures without forking.
- **Sort order.** Use float-style ordering (`sort_order` as a numeric column) so single-row updates are common and full re-numbering is rare. Re-number lazily when ordering precision degrades.
- **Drop target visuals.** Center-band drops highlight the anchor row (.03.01.03); edge-band drops render a placeholder in the destination slot (.03.03.01). Both respect motion-reduce.
- **Optimistic vs. server-confirmed.** Render the move client-side immediately on drop; if the server transaction fails, revert and flash. Don't wait on the round-trip to update UI.
- **Cross-list within-Initiative bookkeeping.** When A moves between Lists, both the source List and the destination List need ancestor-progress recompute (their root-task progress changes), and the Initiative-level aggregate underbar (Arc 2 item 7) updates accordingly.
