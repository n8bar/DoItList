# m03.05 — Documentation & maintenance

_Parent: [`m03-api-mcp.md`](m03-api-mcp.md)_

_Status: **Scoped — pending operator approval.** No implementation until approved._

A reference for the HTTP API, the MCP server, and the scripted client, written for the operator and their agents — not published to the web. Motivated by a stale example that cost a debugging session (`progress` vs `manual_progress`): the contract is only as trustworthy as the docs describing it, and docs nobody maintains rot silently.

**The approach.** Anything mechanical is generated from the code and gated by a test, so it cannot go stale. Anything that needs a human voice — setup, walkthroughs, the why — is hand-written, and only that part relies on care. The generator and the drift guard are one mechanism: the generated text is committed, and a test regenerates it and compares. A stale doc fails the suite.

This arc runs last and then continuously. Public documentation waits for the milestone that exposes the API publicly.

1. [ ] **Home and shape.**

	Settle the layout before writing, so the generator has somewhere to write to.

	1. [ ] Put one file per surface under `docs/specs/`: `api.md`, `mcp.md`, `cli.md`.
	2. [ ] Fence every generated block with begin and end markers that name its source module.
	3. [ ] Say at the top of each file which parts are generated and that hand-edits inside the fences are lost.

2. [ ] **The generator and its gate.**

	One mix task writes every generated block. One test proves the committed text still matches the code.

	1. [ ] **Write `mix doit.docs.gen`.** It rewrites the fenced blocks in place and leaves prose untouched.
		1. [ ] Read the op table from `DoItWeb.Api.Operations` — every wired verb and type, its accepted data keys, and its errors.
		2. [ ] Read response shapes from `DoItWeb.Api.Serializer`.
		3. [ ] Read the MCP tool list from the running adapter's `tools/list`.
		4. [ ] Read the scripted client's verbs and options from `doitlist.py --help`.
		5. [ ] Refuse to write if a fence is missing or unbalanced, naming the file.
	2. [ ] **Gate it with a test.** Regenerate into memory, compare against the committed files, and fail with the diff when they differ.
	3. [ ] Add the task to `mix precommit`, so a contract change that skips the docs cannot land.

3. [ ] **API reference (`api.md`).**

	1. [ ] **Write the auth section.** The `doit_pat_` bearer scheme, where tokens come from, and how they are revoked.
	2. [ ] **Write the error section.** What `401`, `403`, `404`, and `429` each mean here, and what a client should do about each.
		1. [ ] Say plainly that an Initiative with agent access off reads as not found, not forbidden.
	3. [ ] **Write the operations envelope section.** How a batch is shaped, how `lid` forward references work, and what is atomic.
	4. [ ] Drop in the generated op table and the generated response shapes.
	5. [ ] **Write the read and write field split.** Name `progress` as read-only and `manual_progress` as the writable one, the confusion that motivated this arc.

4. [ ] **MCP reference (`mcp.md`).**

	1. [ ] Drop in the generated tool list.
	2. [ ] **Write how each tool maps onto the HTTP API**, so a reader can fall back to the API when a tool is missing.
	3. [ ] **Write the setup section, one client per subsection.** Take the commands from `DoItWeb.AgentConnect` rather than retyping them, so the panel and the docs cannot disagree.
	4. [ ] **Write one end-to-end walkthrough.** Import a document, complete a Task, read the tree back.

5. [ ] **Scripted client reference (`cli.md`).**

	1. [ ] Drop in the generated verb and option list.
	2. [ ] **Write the mirror workflow.** What a mirror is, what the annotations mean, and when a live read is still required.
	3. [ ] **Write the import format.** What the parser treats as a branch, a leaf, and a completion.
	4. [ ] **Write the recovery section.** What to do when a write's outcome is unknown.

6. [ ] **Keep it synced.**

	The generator covers the mechanical half. This is the discipline for the other half.

	1. [ ] Add one line to the engineering style rules: a new endpoint, op, field, or tool is not done until its prose entry is written.
	2. [ ] Add a prose-entry check to the Testing worklist template every future arc starts from.

7. [ ] **Testing and Verification**

	1. [ ] **Pass automated checks.**
		1. [ ] [Agent] The gate test fails on a hand-edited generated block and passes once regenerated.
		2. [ ] [Agent] The generator refuses a file with a broken fence and names it.
		3. [ ] [Agent] `mix precommit` fails when a new op is wired without regenerating.
	2. [ ] **Verify the reference against the running surface.**
		1. [ ] [Guided] Drive a representative slice of the API using only `api.md`, and confirm every documented shape, field, and error matches.
		2. [ ] [Guided] Connect a client using only `mcp.md`, and confirm the walkthrough runs as written.
		3. [ ] [Guided] Work a mirror using only `cli.md`, and confirm the verbs and flags match the script.
	3. [ ] **Verify an agent can work from the docs alone.**
		1. [ ] [Guided] Give an agent only `api.md` and a token, and ask it to build a small tree without the skill or the MCP tools.

## Open Questions

- Should `docs/specs/agent_integration.md` stay separate, or fold into these three files? It describes how an agent should behave; these describe what the surfaces are. Two homes risk drift between them.

## Optional / Future

- A published reference on the domain, with versioning and a contract promise. Waits for the milestone that exposes the API publicly.
- Generated request and response examples captured from real test runs, rather than written by hand.
